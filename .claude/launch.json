{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "api",
      "runtimeExecutable": "bash",
      "runtimeArgs": [
        "-c",
        "set -a; [ -f .env.local ] && . ./.env.local; [ -f frontend/.env.local ] && . ./frontend/.env.local; set +a; export AMICUS_PORT=${AMICUS_PORT:-8010}; exec .venv/bin/python -m api.server"
      ],
      "port": 8010
    },
    {
      "name": "frontend",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["scripts/web.sh"],
      "port": 3000
    },
    {
      "name": "api",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["scripts/api.sh"],
      "port": 8000
    }
  ]
}

